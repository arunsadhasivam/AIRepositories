# requirements.txt
# Install all dependencies with: pip install -r requirements.txt

# RAGAS — RAG evaluation framework (Faithfulness, Context Precision, Answer Correctness)
ragas==0.1.7

# HuggingFace Datasets — required by RAGAS to format evaluation data
datasets==2.18.0

# BERTScore — semantic similarity evaluation using BERT embeddings
bert-score==0.3.13

# Transformers — required by BERTScore for loading BERT models
transformers==4.40.0

# PyTorch — backend for BERTScore model inference
torch==2.2.2

# Langfuse — observability and evaluation tracking platform
langfuse==2.36.1

# OpenAI — used by RAGAS internally as the default LLM judge
# Replace with Azure OpenAI config if using Azure
openai==1.30.1

# python-dotenv — load environment variables from .env file
python-dotenv==1.0.1
