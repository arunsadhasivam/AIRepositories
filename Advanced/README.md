Layer1 Tool validation
======================
- parameter valdation(types,ranes, format))
- schema validation(nested fields, enums, business typez)
- policy checks(user,roles,actions)
- budget validations (per request,per user, per day)
- authorization( row level rights on target entity)


Layer2 giadtrails:
===================

- prompt guadrails(injection, scope,policy)
- output guadrails(profanity, jailbreaks,fabrication)
- compliance che c ks(domain specific rules)
- pii protection(detect,mask,audit)
- hallucinationbreduction(citatoon validation)

Layer3 observability:
=